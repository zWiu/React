export default () => {
     const status = true 
     return (
        <div>
            <p style={{
                color: status ? "#00ff9f" : "#f64348"
            }}>Está é uma página 
                com o intuito de 
                ser a sua favotira.</p>              
        </div>
    )
}